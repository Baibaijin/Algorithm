今天做的题属于有点思路，自己能够推出一部分，但是不能完全推出来
比如数的计算和小A点菜这两道DP问题，能够相处状态表示，但是状态计算这部分还是有点问题
状态转移方程列不出来
顺便复习了一下二分算法
太久没有敲，有点生疏了
学会了运用
lower_bound(start,end,k),寻找k元素出现的第一个位置,
upper_bound(start,end,k)比k元素大的第一个元素位置,
binary_search(start,end,k),找某个元素是否出现.
需要注意，upper_bound和lower_bound都是返回地址，要减去arr。
